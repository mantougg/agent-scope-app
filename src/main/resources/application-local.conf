# 本地配置覆盖文件（gitignored，不会进 git）
# - AppConfig 启动时会先加载本文件，键值会覆盖 application.conf 里的同名键
# - 只写你想覆盖的字段即可，其余从 application.conf 继承
# - HOCON 语法：https://github.com/lightbend/config/blob/main/HOCON.md
#
# 常见用法：本地试用别的模型 / 自己的接入点 / 改 baseUrl

model {
  name    = "kimi-k2.6"
  baseUrl = "https://ark.cn-beijing.volces.com/api/coding/v3"
}
